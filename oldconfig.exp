#!/usr/bin/expect -f
spawn make oldconfig
set timeout -1
expect {
    -re {.*\[.*[yYnNmM].*\].*} { send "n\r"; exp_continue }
    -re {.*\[.*[0-9]+.*\].*}   { send "\r"; exp_continue }
    -re {.*: $}                { send "\r"; exp_continue }
    eof
}
